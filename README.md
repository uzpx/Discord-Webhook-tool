# Discord-Webhook-tool
FOr educational purposes only.
Requirements = requests>=2.28.0,<3.0 // pip install requests

If you are on windows 11, use run.bat, if you are on windows 10 then use PNR @ xr4o.bat. (idk for older versions, most likely PNR @ xr4o.bat)
